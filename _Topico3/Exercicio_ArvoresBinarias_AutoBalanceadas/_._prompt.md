# Prompt Codex

```prompt

Implemente seguindo RIGOROSAMENTE o descrito no enunciado "ArvoresBinarias_AutoBalanceadas.png".
A primeira célula em markdown deve descrever um Resumo do Código implementado. Nessa mesma célula (ao final) ter uma lista simplificada da Estrutura do notebook.
Separar as células, uma para cada tipo de busca em profundidade "em-ordem", "pré-ordem" e "pós-ordem".

Mostrar visualmente de forma didática a execução de cada tipo de busca. Deixar esse código (mostrar visualmente) em células separada das células de código que implementa a busca em si.

Dividir as funções em células para melhorar a legibilidade da implementação, e sempre colocar acima de cada célula de código uma descrição do que é feito na célula de código, na mesma célula de código Python.
E, a última célula em markdown mostrar o resultados pedidos no enunciado do exercício.
```

```prompt

Acrescente o comentário da ordem de complexidade de cada um dos algoritmos de busca em profundidade ("em-ordem", "pré-ordem" e "pós-ordem") nas células de código em Python dos respectivos algoritmos.
```
