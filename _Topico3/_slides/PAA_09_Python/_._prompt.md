# Prompts

```terminal
cd .venv;pip freeze > ../dependencias.txt
```

```prompt

Utilize o enunciado dos slides "Dijkstra_contexto.png" e "Dijkstra_algoritmo.png" para implementar o código necessário para desenvolver o algoritmo de busca Dijkstra no notebook main.ipynb.
Use o exemplo de grafo mostrado em "Dijkstra_contexto.png" para executar o algoritmo.
Siga o pseudo código de "Dijkstra_algoritmo.png" para desenvolver o algoritmo Dijkstra.

Separar as funções em células de forma a melhorar o entendimento, seguindo a mesma sequência de explicação utilizada nos slides, onde cada célula deve ter um comentário explicativo acima.

A primeira célula em markdown deve descrever um Resumo do Código implementado. Nessa mesma célula (ao final) ter uma lista simplificada da Estrutura das células do notebook.

A cada célula de código (quando possível) acrescentar o custo computacional desse código os comentários. No final ter uma célula markdown com uma estatística do custo total dos códigos.

Sempre que possível acrescentar a plotagem, se julgar necessário, para melhorar o entendimento didático do código usando SVG/HTML.
```

```prompt

Crie um enunciado para uma atividade para desenvolver esse código mencionando para usar o artigo "artigo_bolas_DocPlayer.pdf", seguindo rigorosamente as funções usadas na artigo. A atividade deve perver poder usar o código por uma imagem (imagemTeste.png) como pela webcam, como esse código faz.
Colocar esse enunciado como a primeira célula texto (markdown).
Mas, manter o que já se tem. Não mudar nada das células atuais.
```

```prompt

Colocar um retângulo de cor verde para esconder TODO código Python do arquivo "main.pdf" (que foi gerado do main.ipynb), mas deixe as linhas de comentários, gerando um novo arquivo PDF com nome "Trabalho_Unidade3.pdf". A ideia é esse PDF servir de gui para se fazer a atividade, mas não entregar a resposta dos código desenvolvido. Também esconder com um retângulo de cor verde onde aparecer as palavras: cvInRange, cv2.GaussianBlur, cv2.HoughCircles, cv2.inRange, cvHoughCircles, cvInRange, cvSmooth, HoughCircles, inRange e Smooth.
```
