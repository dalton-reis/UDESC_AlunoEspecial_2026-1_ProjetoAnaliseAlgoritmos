# Prompt Codex

```prompt
Implemente o código para gerar um "hash" usando SHA-256 para ler um arquivo, gerar a chave hash e mostrar aqui, essa chave também deve ser salva em um novo arquivo com o mesmo nome do arquivo lido, mas com uma extensão diferente.
Depois ter como ler um novo arquivo, e ler o arquivo da chave hash, e assim verificar se o arquivo lido é daquela chave hash.
Separar as funções em células de forma a melhorar o entendimento, onde cada célula deve ter um comentário explicativo acima na própria célula do código em Python.
Na primeira célula ("Resumo do código") ter uma explicação o que esse código faz. Seguido de uma lista estruturada o que as células fazem.
```