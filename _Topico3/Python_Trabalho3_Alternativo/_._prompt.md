# Prompt Codex

```prompt

Implemente seguindo RIGOROSAMENTE o descrito no enunciado "PAA_TrabalhoPratico3_ArvoreTrie_Alternativo.pdf".

O trabalho consiste em implementar e um algoritmo para auxílio na solução do jogo Termo usando a estrutura de dados chamado Árvore Trie. O programa deve ler um dicionário e montar a árvore. Após lido o dicionário, o programa deve permitir que o usuário escreva uma palavra de teste e as cores retornadas pelo website https://term.ooo (Amarelo, Verde ou Preto). A partir da palavra de teste (chute) e da lista de cores, o programa deve sugerir as possíveis palavras usando a árvore Trie gerada a partir do dicionário.
O objetivo deste trabalho é permitir ao aluno implementar um algoritmo para previsão de palavras para auxiliar na resolução do jogo Termo.
Adicionalmente, você também poderá utilizar as respostas anteriores para melhorar a resposta do programa. Também é possível gerar uma versão do Termo offline para fazer o computador “jogar sozinho” mostrando as tentativas e possíveis soluções de forma iterativa.

Utilizar como dicionário um conjunto de palavras reservadas da linguagem de programação Python. Esse dicionário é para montar para montar a árvore. Ter uma rotina separada em uma célula para exibir a árvore montada usando SVG/HTML.

Sempre que for possível, mostrar a execução dos algoritmos usando SVG/HTML para ter uma forma didática para entender a sua execução. Mas separar esses códigos em células separadas para ser for preciso poder executar os algoritmos sem ter essa visualização.

Acrescente o comentário da ordem de complexidade dos algoritmos utilizados nas células de código em Python dos respectivos algoritmos.

A primeira célula em markdown deve descrever um Resumo do Código implementado. Nessa mesma célula (ao final) ter uma lista simplificada da Estrutura das células do notebook.

Mostrar visualmente de forma didática a execução de cada tipo de busca. Deixar esse código (mostrar visualmente) em células separada das células de código que implementa a busca em si.

Dividir as funções em células para melhorar a legibilidade da implementação, e sempre colocar acima de cada célula de código uma descrição do que é feito na célula de código, na mesma célula de código Python.
E, a última célula em markdown mostrar o resultados pedidos no enunciado do exercício.
```
