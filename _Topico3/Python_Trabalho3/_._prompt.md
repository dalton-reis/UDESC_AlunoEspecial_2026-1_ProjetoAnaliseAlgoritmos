# Prompt Codex

```prompt

Implemente seguindo RIGOROSAMENTE o descrito no enunciado "PAA_TrabalhoPratico3_ArvoreTrie.pdf".

O objetivo deste trabalho é permitir ao aluno implementar um algoritmo para previsão de palavras a partir do início da escrita para auxílio na tarefa de autocompletar.
O trabalho consiste em implementar e um algoritmo para auxílio na tarefa de autocompletar usando a estrutura de dados chamado Árvore Trie. O programa deve ler um dicionário e montar a árvore. Após lido o dicionário, o programa deve permitir que o usuário escreva o início de uma palavra. A partir do início, o programa deve sugerir quais palavras o usuário pretende escrever.

Utilizar como dicionário um conjunto de palavras reservadas da linguagem de programação Python. Desta forma, o programa pode se comportar como uma IDE que auxilia o programador a digitar mais rápido. Esse dicionário é para montar para montar a árvore.
Ter uma rotina separada em uma célula para exibir a árvore montada usando SVG/HTML.

A primeira célula em markdown deve descrever um Resumo do Código implementado. Nessa mesma célula (ao final) ter uma lista simplificada da Estrutura das células do notebook.

Mostrar visualmente de forma didática a execução de cada tipo de busca. Deixar esse código (mostrar visualmente) em células separada das células de código que implementa a busca em si.

Dividir as funções em células para melhorar a legibilidade da implementação, e sempre colocar acima de cada célula de código uma descrição do que é feito na célula de código, na mesma célula de código Python.
E, a última célula em markdown mostrar o resultados pedidos no enunciado do exercício.
```

```prompt

Acrescente o comentário da ordem de complexidade dos algoritmos utilizados nas células de código em Python dos respectivos algoritmos.
```
