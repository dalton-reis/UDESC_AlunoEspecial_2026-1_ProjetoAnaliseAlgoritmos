# Prompt Codex

```prompt

Implemente em células separadas as seguintes estruturas Pyrhon:
list, collections.deque, sortedcontainers, set e dict.

Para cada estrutura simular um teste para as operações de inserir, pesquisar e remover elementos da estrutura. E, na descrição do comentário no inicio da célula também colocar a ordem de complexidade para cada operação.

A primeira célula em markdown deve descrever um Resumo do Código implementado. Nessa mesma célula (ao final) ter uma lista simplificada da Estrutura das células do notebook.

Mostrar visualmente de forma didática a execução de cada tipo de estrutura. Deixar esse código (mostrar visualmente) em células separada das células de código que implementa a busca em si.

Dividir as funções em células para melhorar a legibilidade da implementação, e sempre colocar acima de cada célula de código uma descrição do que é feito na célula de código, na mesma célula de código Python.
```
